function List() {
  let items = ["Leche", "Huevos", "Carne", "Pan", "Mantequilla", "Queso"];
  return (
    <div>
      <ul class="list-group">
        {items.map((item) => (<}

      </ul>
    </div>
  );
}

export default List;
